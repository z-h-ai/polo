FROM debian:bookworm-slim

# The release workflow replaces this placeholder deployment with a temporary
# context containing the compiled publisher and one curated release directory.
CMD ["sleep", "infinity"]
