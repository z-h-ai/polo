# 非交互 exec 默认使用安全模式

`polo exec` 未提供危险授权参数时默认使用 `safe`，P0 不提供可能无限等待的交互式审批，需要审批的修改操作必须明确失败；`-C` 只设置执行起点，不构成 sandbox。`--yolo` 与危险长别名显式切换为 Polo 的 `allow-all`，但文档不会把该应用层权限模式描述为 Codex 操作系统 sandbox 的完整等价物。默认可写虽然更方便，却不适合无界面的脚本和 CI。
