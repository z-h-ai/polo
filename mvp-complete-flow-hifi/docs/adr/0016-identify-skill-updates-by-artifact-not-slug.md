---
status: accepted
---

# Identify Skill updates by Artifact, not slug

A Skill Update exists only between versions of the same opaque `artifactId`; an equal slug from another Creator Space is a conflict-driven source replacement. This prevents one publisher from impersonating another publisher's update stream while still allowing a user to explicitly replace a same-slug workspace Skill.
