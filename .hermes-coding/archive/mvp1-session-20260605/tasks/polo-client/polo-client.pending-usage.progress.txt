# Task Progress: polo-client.pending-usage
[2026-06-05T19:25:52.469Z] PendingUsageStore uses synchronous readFileSync (imported statically from 'fs') in the constructor to load JSONL from disk — Bun.file().text() is async and cannot be awaited in a constructor. Bun.write() is used for full file rewrites (remove/markRetry), and appendFileSync for efficient new-entry appends. The singleton is instantiated at module load time so CONFIG_DIR is resolved from POLO_AI_CONFIG_DIR at import time — test isolation requires createPendingUsageStore({ filePath }) with a tmpdir path, not env-var overrides.
