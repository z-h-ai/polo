# Scene traceability audit

This is the per-catalog-scene audit required for the reusable baseline. The
baseline acceptance mode is **static source derivation**: each scene is
translated from the listed Renderer source, its imported visual assets, CSS and
fixed strings. A `partial` reference proves only the named production component,
not an overall application page. No row below treats a prototype screenshot as
Renderer proof.

| Catalog scene | Source-backed implementation | Renderer source path(s) | Real Renderer / Playground reference | Audit status |
| --- | --- | --- | --- | --- |
| `splash` | `src/source/LifecycleRegion.jsx` | `components/SplashScreen.tsx`, `components/icons/PoloAiSymbol.tsx` | none | static source-derived |
| `onboarding` | `src/source/AdminLogin.jsx`, `src/source/OnboardingAuth.jsx`, `src/source/LifecycleRegion.jsx`, `src/source/OnboardingSteps.jsx` | `components/onboarding/{OnboardingWizard,AdminLoginStep,WelcomeStep,AdminKickedStep,CompletionStep,GitBashWarning,ProviderSelectStep,APISetupStep,LocalModelStep,CredentialsStep,AdminLoginMethodTabs,PhoneAuthStep,phone-auth-utils}.tsx`, `components/onboarding/primitives.tsx`, `components/apisetup/ApiKeyInput.tsx` | `reference-renderer-login-1440x900.png`; `reference-playground-onboarding-{welcome,wizard}-1440x900.png` | static source-derived; admin-login (invalid-credentials fixture)/Welcome/AdminKicked/Completion plus all wizard steps (git-bash cards, provider-select cards, api-setup segmented control with the api-key option selected, api-setup-pi with the GitHub Copilot card on the Polo AI Backend segment, local-model defaults, credentials api-key/copilot-code/claude-code states) and the phone-auth states (AdminLoginMethodTabs + PhoneAuthStep entry — +86 prefix, consent, submittable 发送验证码 — and verify — masked `+86 138 •••• 8000` from maskMainlandPhone, 6-digit code field, frozen 43s 重发 countdown, disabled 继续); provider card icons are runtime_exact SVG assets |
| `reauth` | `src/source/LifecycleRegion.jsx` | `components/onboarding/ReauthScreen.tsx` | none | static source-derived |
| `workspace-picker` | `src/source/WorkspacePicker.jsx`, `src/source/WorkspaceSteps.jsx` | `components/workspace/{WorkspacePicker,WorkspaceCreationScreen,AddWorkspaceStep_Choice,AddWorkspaceStep_CreateNew,AddWorkspaceStep_OpenFolder,AddWorkspaceStep_ConnectRemote,AddWorkspace_RadioOption,primitives}.tsx`, `components/ServerDirectoryBrowser.tsx` | none | static source-derived; empty/loading fixture plus the three-step creation flow (`create` custom-location + `open-folder` pre-picked + `connect-remote` connected/select branches and `connect-remote-create` create-on-server branch, hardcoded English labels kept literal) and the `server-browser` dialog with a truncated (6/42) symlink-bearing listing fixture |
| `home` | `src/source/HomeLauncher.jsx`, `src/source/HomeTabFrame.jsx` | `components/tab-browser/{HomePage,TabBar,TabContent,AppIcon,OrganizationAppCard}.tsx` | none | static source-derived; fresh-profile fixture (runtime catalog is data-dependent) |
| `enterprise-home` | `src/source/HomeLauncher.jsx`, `src/source/HomeTabFrame.jsx`, `src/source/HomeApps.jsx` | `components/tab-browser/{HomePage,OrganizationAppCard}.tsx` | none | static source-derived; catalog fixture (runtime organization data is data-dependent); `organization-apps` state renders the OrganizationAppCard grid (installed status, initial-letter artwork gradient) |
| `space-switcher` | `src/source/SpaceSwitcher.jsx` | `components/app-shell/{WorkspaceSwitcher,CompactWorkspaceSwitcher,TopBar}.tsx`, `components/organization/OrganizationSwitcher.tsx`, `packages/ui/src/components/ui/{StyledDropdown,drawer}.tsx` | none | static source-derived; both WorkspaceSwitcher trigger variants (topbar pill `h-[30px] px-3 rounded-[8px] border-foreground/6` with unread dot, sidebar bottom slot with FadingText) and the open dropdown on the StyledDropdown chrome (min-w-64 topbar / min-w-40 sidebar, active `bg-foreground/10`, Cloud/CloudOff rows, hover-reveal Trash2/ExternalLink shown on a named hover fixture, separator + 添加 Workspace… FolderPlus row), CompactWorkspaceSwitcher's vaul bottom drawer (overlay bg-black/50, handle, centered 选择 Workspace header, h-7-avatar rows with remote meta urls / 已断开 reconnect copy and the mt-1 add row) and OrganizationSwitcher (Sparkles/Building2 triggers, min-w-56 menu with Owner/Member role labels, 作品/管理组织/创建另一个组织 trailing rows); workspaces, remote urls, unread map, health results and org summaries are named deterministic fixtures (menu rows/icons follow zh-Hans workspace./sidebarMenu./organization. keys; role labels Keep English like the locale file) |
| `chat` | `src/source/EmptyChat.jsx`, `src/source/ChatConversation.jsx`, `src/source/ChatStreaming.jsx`, `src/source/PoloShell.jsx` | `pages/ChatPage.tsx`, `components/{chat/ChatDisplay,chat/ChatMessageRow,chat/MessageBubble,chat/ErrorMessage,chat/EmptyStateHint,app-shell/input/ChatInputZone,app-shell/input/InputContainer}.tsx`, `packages/ui/src/components/chat/{UserMessageBubble,TurnCard,TurnCardActionsMenu,ResponseCard,ResponseContent,ActivityStatusIcon,TodoList}.tsx`, `packages/ui/src/lib/layout.ts` | `reference-playground-chat-{empty-hint,input}-1440x900.png` | static source-derived; key production components independently referenced; `conversation` state renders user/assistant bubbles on the CHAT_LAYOUT column with a named transcript fixture; `streaming` state renders a running TurnCard (collapsed header with step-count badge + preview text, expanded `border-l-2 border-muted ml-[13px]` timeline: intermediate row spinner + 思考中, tool row spinner + `Bash · git log --oneline -14`) above the streaming ResponseCard (throttled partial markdown, `bg-muted/20` footer with spinner + `Streaming...`); `error` state renders the chat error system turn — MessageBubble error branch: destructive-tinted bubble (`oklch(from var(--destructive) l c h / 0.03)` + shadow-tinted), `模型服务超时` title, error body, 重试 action and the expanded 隐藏技术详情 block (`Raw: request timed out…` mono rows on `border-destructive/20`); `detail` state renders a completed turn in full: expanded timeline (完成后 intermediate → MessageCircleDashed `整理 changelog 条目结构`, Bash → CheckCircle2, Write → accent FilePenLine), the TodoList section (completed CircleCheck accent / in-progress spinner / pending Circle rows under `Todo List`) and the completed ResponseCard (maxHeight 540 scrollable content with hover-fullscreen button slot, footer `复制`/`Markdown` actions + GitBranch); `active-tasks` state renders the ActiveTasksBar fixture above a chat input area (agent + shell badges — `任务|Shell` label, mono task ids `tsk_9f3a2c1e…`/`sh-8b1d4f`, tabular elapsed timers, chevron — with the open StyledDropdown menu: 查看输出 ArrowUpRight / separator / 停止任务 Square); `auth-request`/`auth-request-basic`/`auth-completed`/`auth-cancelled` states render the inline AuthRequestCard (pending credential form: title/description from the Notion fixture, single API Key field with Key adornment + Eye toggle, `Enter api key` placeholder, `Credentials are encrypted at rest` hint, Save disabled while empty, Cancel ghost and the trailing hint in the `border-t border-border/50` action bar; basic-auth variant renders Username + Password fields; completed = success wash + CheckCircle2 `Notion Connected` + `Signed in as demo@example.com`; cancelled = muted wash + XCircle `Notion Cancelled` — English copy kept literal as in source); `fab` state renders the FabNewChat floating button (52.5px accent circle `right-15 bottom-15` shadow-tinted, aria-label 新建聊天) |
| `chat-permission` | `src/source/EmptyChat.jsx`, `src/source/PoloShell.jsx` | `pages/ChatPage.tsx`, `shared/agent/mode-types.ts` | `reference-playground-chat-input-1440x900.png` | static source-derived; permission data is fixture-bound |
| `sources` | `src/source/ResourceEmptyPanels.jsx`, `src/source/ResourceListDetail.jsx`, `src/source/PoloShell.jsx` | `components/app-shell/SourcesListPanel.tsx`, `components/app-shell/SourceMenu.tsx`, `components/ui/{entity-list-empty,empty,entity-list-badge}.tsx`, `components/ui/source-avatar.tsx`, `pages/SourceInfoPage.tsx`, `components/info/{Info_Page,Info_Section,Info_Table,Info_Markdown,ToolsDataTable,PermissionsDataTable,Info_Badge,Info_StatusBadge}.tsx`, `components/ui/EditPopover.tsx` | `reference-playground-entity-sources-1440x900.png` | static source-derived; empty fixture with Learn-more + add CTA pair, `list` state on a named 3-source dataset (per-type fallback icon + EntityListBadge type/status tints: MCP accent/需要认证 warning/本地 info), `detail`/`detail-menu` states rendering SourceInfoPage on an MCP fixture — connection Info_Table (type/URL button/上次测试, frozen 2-day relative time), ToolsDataTable (允许/询问 access badges), PermissionsDataTable (已阻止 patterns) and guide.md markdown preceded by the hover-reveal fullscreen button; menu actions follow SourceMenu (在新窗口中打开/在 Finder 中显示/发送到 Workspace.../删除数据源); source metadata, urls, statuses, tool/permission rows and guide are named deterministic fixtures |
| `skills` | `src/source/ResourceEmptyPanels.jsx`, `src/source/ResourceListDetail.jsx`, `src/source/PoloShell.jsx` | `components/app-shell/SkillsListPanel.tsx`, `components/app-shell/SkillMenu.tsx`, `components/ui/{entity-list-empty,empty}.tsx`, `components/ui/skill-avatar.tsx`, `pages/SkillInfoPage.tsx`, `components/info/{Info_Page,Info_Section,Info_Table,Info_Markdown}.tsx`, `components/ui/EditPopover.tsx` | `reference-playground-entity-skills-1440x900.png` | static source-derived; empty fixture with Learn-more + add CTA pair, `list` state on a named 3-skill dataset (emoji avatar, 项目 project badge, 有可用的安全版本 update badge), `detail`/`detail-menu` states rendering SkillInfoPage on a creator-installed workspace fixture — update banner (更新/忽略这个版本), hero, metadata Info_Table (标识符/名称/描述/数据源/位置 with skills-relative path button/建议的 Source), requested-tools code chips and SKILL.md instructions markdown; SkillMenu with the disabled 由项目管理 delete row for non-workspace skills; skills metadata, paths and markdown are named deterministic fixtures |
| `automations` | `src/source/ResourceEmptyPanels.jsx`, `src/source/AutomationsRegion.jsx`, `src/source/PoloShell.jsx` | `components/automations/{AutomationsListPanel,AutomationInfoPage}.tsx`, `components/automations/types.ts`, `components/app-shell/MainContentPanel.tsx`, `components/ui/{entity-row,entity-list-empty,empty}.tsx`, `components/info/{Info_Page,Info_Section,Info_Table}.tsx` | `reference-playground-automations-list-1440x900.png` | static source-derived; empty fixture plus `list`/`detail` states on a named automation dataset (EntityRow + MicroBadge rows, Info_Section hero/当/如果/则/设置 with Info_Table rows); event/permission badge naming follows EVENT_DISPLAY_NAMES and PERMISSION_DISPLAY_NAMES |
| `browser` | `src/source/BrowserEmptyState.jsx`, `src/source/BrowserRegion.jsx` | `browser-empty-state.tsx`, `components/browser/empty-state-prompts.ts`, `packages/ui/src/components/ui/{BrowserEmptyStateCard,BrowserControls}.tsx`, `components/browser/{BrowserTabBadge,BrowserTabStrip}.tsx` | `reference-playground-browser-empty-1440x900.png`; `reference-playground-browser-tab-strip-1440x900.png` | static source-derived; empty card (zh-Hans title/description/safety-hint, English sample prompts as in source) plus `toolbar` state (nav cluster, URL field, loading-idle branch) and tab-strip badges; BrowserView is approximate |
| `app-menu` | `src/source/AppMenu.jsx` | `components/{AppMenu,app-menu/DesktopAppMenu}.tsx`, `packages/ui/src/components/ui/StyledDropdown.tsx`, `shared/menu-schema.ts` | none | static source-derived; StyledDropdown item/shortcut/separator metrics; action handlers are IPC boundaries |
| `settings` | `src/source/SettingsRegion.jsx`, `src/source/PoloShell.jsx` | `pages/settings/{SettingsNavigator,AppSettingsPage,AppearanceSettingsPage,InputSettingsPage,WorkspaceSettingsPage,PermissionsSettingsPage,LabelsSettingsPage,MessagingSettingsPage,ServerSettingsPage,PreferencesPage,ShortcutsPage,AccountSecuritySettingsPage}.tsx`, `components/settings/*`, `components/icons/SettingsIcons.tsx`, `components/app-shell/PanelHeader.tsx`, `shared/settings-registry.ts`, `renderer/actions/{definitions,registry}.tsx` | none | static source-derived; every settings page has its own scene state (fresh-profile navigator keeps account-security/server gated out; both still render behind `account-security`/`server` states), IPC defaults and empty branches; shortcuts page mirrors the action-registry hotkey table |
| `organization` | `src/source/OrganizationOnboarding.jsx`, `src/source/OrganizationManage.jsx` | `components/organization/{OrganizationOnboarding,OrganizationManagementDialog,CreatorArtifactsPanel}.tsx` | none | static source-derived; loading/create fixture; join/select render card frames with the IPC-preview slot (spinner body); `members`/`invitations` states render the management dialog tabs on named fixtures; `artifacts` state renders CreatorArtifactsPanel's `grid min-h-[440px] md:grid-cols-[220px_1fr]` layout — aside (作品 header + RefreshCw, create-artifact box with the closed 作品类型 select on Skill + 新建 Skill 草稿, scrollable artifact rows with selected `border-accent/40 bg-accent/5`), detail section (h2 + summary + status pill, published-version Select v1.2.0 + changelog/checksums mono/包大小 box with the install target and 安装 button, 1.3.0-rc draft box 校验中 + 选择 ZIP 并上传, validation issue rows in error/warning variants, metadata grid, SKILL.md + 文件(N) details, revoke row with 必填撤销原因 input and disabled 撤销, empty web-app detail path for the Report Web fixture); artifact/version/workspace target/issue/file data are named deterministic fixtures |
| `shortcuts` | `src/source/KeyboardShortcutsDialog.jsx` | `components/KeyboardShortcutsDialog.tsx`, `renderer/actions/definitions.ts` | none | static source-derived; registry + component-specific sections, Mac hotkey display |
| `conversations` | `src/source/SessionSidebar.jsx` | `components/app-shell/{SessionList,SessionItem,SessionMenu,SessionMenuParts,SessionSearchHeader,SessionStatusIcon}.tsx`, `components/ui/{entity-list,entity-row,entity-list-badge,entity-list-empty}.tsx`, `config/session-status-config.tsx`, `utils/session.ts`, `packages/shared/src/statuses`, `packages/shared/src/colors/defaults.ts` | none | static source-derived; every grouping mode (date / status / unread buckets) with collapsible group headers and collapsed counts, search mode (highlighted titles + match-count badges + results row + 当前视图/其他对话 groups), the row dropdown (SessionMenu actions on the StyledDropdown chrome with sub-triggers) and the entity-list empty screen; statuses are the shared default todo-state set (DEFAULT_STATUS_COLORS/DEFAULT_ICON_SVGS, resolved for light), sessions/bindings/match counts/times are named deterministic fixtures |
| `session-info` | `src/source/SessionInfoRegion.jsx` | `components/app-shell/SessionInfoPopover.tsx`, `components/files/FileViewer.tsx` | none | static source-derived; `popover` state renders the 360px floating popover (会话信息 title labeelled field with the 无标题 session-name input + accent save chip, SessionFilesSection header 会话文件 + 在 Finder 中查看 ghost action, expanded fixture file tree — expandable Folder rows with 13.125px chevron/Folder icons, 14px-level File rows, connector lines at `left-[13px]`, SKILL.md selected — and hover-reveal 打开/在 访达 中显示 row actions; `${fileManager}` resolves to 访达 per the locale placeholder); `viewer` state renders FileViewer's mono selectable path header (`px-4 py-3 bg-muted/50`) + scrubbed pre content; `viewer-empty` renders the 60px FileText tile + 未选择文件 + 点击聊天中的文件路径在此查看 (i18n keys); session title, file tree, expand state and selected path are named deterministic fixtures (IPC events unwired) |
| `multi-select` | `src/source/EdgePanels.jsx` | `components/app-shell/{MultiSelectPanel,BatchSessionMenu}.tsx` | none | static source-derived; `panel` state renders the full-height selection screen (60px `accent/10` circle with count `3`, `已选择 3 个会话`, Kbd hint rows ⌘/⇧ 点击 and 按 Esc 清除选择, ghost batch actions 更改状态/设置标签/发送到 Workspace.../归档); `menu` state renders the BatchSessionMenu context menu on the StyledDropdown chrome (已选择 N 个会话 header, expanded 状态 submenu with 待办 checked + 进行中/已完成/已归档, 标签 submenu trigger, 全部标记 info Flag, 归档, 发送到 Workspace..., separator, destructive 删除 Trash2); selection count and status set are named deterministic fixtures |
| `send-remote` | `src/source/EdgePanels.jsx` | `components/app-shell/SendToWorkspaceDialog.tsx` | none | static source-derived; dialog on the shared dialog chrome (sm:max-w-sm = 384px, Send+发送到 Workspace title, `将 3 个会话发送到远程 Workspace。` description, footer Cancel/Send kept literal English as in source); `list` state shows the 3 workspace fixture rows (dev-server/staging ok connected, edge-lab error → CloudOff, disabled at 50%); `offline` state shows all rows disconnected; `transferring` shows `正在将…` description, disabled Cancel, `Sending...` primary with the purple #8B5CF6 LED progress ring; the transferring spinner is the SendToWorkspaceDialog unconditional interval spinner recreated as an inline SVG mounted on the button |
| `messaging` | `src/source/MessagingDialogs.jsx` | `components/messaging/{TelegramConnectDialog,WhatsAppConnectDialog,LarkConnectDialog,PairingCodeDialog,TelegramSupergroupPairingDialog}.tsx` | none | static source-derived; dialog family on the dialog.tsx chrome — telegram (token success fixture + reconfigure idle) / whatsapp (QR SVG from a fixture payload + connected-as) / lark (region selector + filled secrets + test-ok) / pairing-code and supergroup-pairing (6-digit codes with frozen countdown snapshots, bot deep links); QR layout uses the same qrcode.react package as the Renderer |
| `reset` | `src/source/ResetDialog.jsx` | `components/ResetConfirmationDialog.tsx` | none | static source-derived; source challenge regenerated on open |

## Component gallery

`components/` is the component-level counterpart of `prototype.html`, exported
by `tools/export-component-gallery.mjs`. Each `components/<group>/<component>.html`
is a self-contained static SSR translation of one `src/source/*` component
(inline tokens.css/base.css, assets as data URLs, no scripts, no network),
grouped by the catalog groups in `scene-catalog.json`: lifecycle, workbench,
assistant, resources, apps, settings, organization, system. `components/index.html`
is the classification overview. Gallery pages are review artifacts; the editable
source of truth remains `src/source/`.

## Boundary rule

Only Electron IPC, BrowserView/native browser process, and OS-native UI are
classified as approximate. Runtime screenshots are optional validation evidence
in this mode; their absence does not downgrade a statically derived scene. Data
that the source cannot uniquely determine (account, organization, catalog,
conversation and permission state) is represented only by an explicitly named,
deterministic source-compatible fixture.
